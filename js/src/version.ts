// SDK version. Kept in a tiny file mirroring `python/src/agent_hub_sdk/version.py`
// so both ports advertise the same number. Bump on each release tag.
// Keep in sync with python/src/agent_hub_sdk/version.py — the polyglot
// SDK ports advertise the same version number.
export const VERSION = "0.5.0";
