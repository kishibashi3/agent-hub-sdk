// agent-hub-sdk — Unified TypeScript SDK for agent-hub.
//
// Status: M0 (skeleton). Public surface is intentionally minimal — only the
// VERSION constant is exported. M4 will add `AgentHub`, error classes, and
// `InboxMessage` after porting the Python core (M1+M2) to TypeScript.
//
// See `docs/design.md` for the full plan.

export { VERSION } from "./version.js";
